@model List<SIM_Lubricentro.Models.Carro>
@{
    ViewData["Title"] = "Lista Carros";
}

<h1>Lista Carros</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
@using (Html.BeginForm())
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @*@Html.DisplayNameFor(model => model.Placa)*@
                    Placa
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Vehiculo)*@
                    Vehiculo
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Estilo)*@
                    Estilo
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Año)*@
                    Año
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Kms)*@
                    Kms
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.Cliente)*@
                    Cliente
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
             <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {

            <tr>
                @Html.HiddenFor(modelItem => Model[i].ID)
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Placa)
                        @Html.HiddenFor(modelItem => Model[i].Placa) @*este es para que se pueda obtener en el objeto*@
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Vehiculo)
                        @Html.HiddenFor(modelItem => Model[i].Vehiculo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Estilo)
                        @Html.HiddenFor(modelItem => Model[i].Estilo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Año)
                        @Html.HiddenFor(modelItem => Model[i].Año)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Kms)
                        @Html.HiddenFor(modelItem => Model[i].Kms)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model[i].Cliente.Nombre)
                        @Html.HiddenFor(modelItem => Model[i].Cliente.ID)
                    </td>
                    <td>
                        @*<form asp-controller="Carros" asp-action="Index" method="get">*@ @*esto es para mandarlo a otro controlador*@
                            
                        <input type="submit" value=" Agregar  " class="btn btn-primary" asp-route-id="@Model[i].ID" asp-controller="RealizarReparaciones" asp-action="Realizar_Reparacion_Carro" method="get" /> @*aqui lo que hago es enviar este producto al controlador RealizarReparaciones y al metodo Realizar_Reparacion_Carro que es donde va a estar los datos que se quieren realizar

                        @*</form>*@





                        @*<input type="submit" value=" Agregar  " class="btn btn-primary" asp-route-id="@Model[i].ID" />*@ @*aqui lo que hago es enviar este producto a la lista de deseo*@
                        

                        @*<a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
}